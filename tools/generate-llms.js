// Placeholder script for generate-llms
// This script runs before vite build but is not required
// The build will continue even if this fails (see package.json build script with || true)

console.log('Generate LLMs script placeholder — no-op.')
